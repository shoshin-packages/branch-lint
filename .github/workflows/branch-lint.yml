name: Branch Lint

on:
  push:
    branches-ignore:
      - master
      - dev

jobs:
  lint:
    name: Validate branch name
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Validate branch name
        run: node index.js
